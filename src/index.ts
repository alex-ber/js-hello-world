const greeting: string = "Hello, Kate!";
const unusedVar = 42; // ESLint should complain about unused variable

console.log(greeting); // TypeScript LSP should suggest autocompletion to 'greeting'

const a=17;
